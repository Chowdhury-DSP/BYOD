#pragma once

#include <pch.h>

namespace GUIConstants
{
constexpr int defaultWidth = 1000;
constexpr int defaultHeight = 600;

namespace Colours
{
    const Colour backgroundGrey { 0xFF2A2A2A };
    const Colour barBackgroundShade { 0x3A000000 };

    const Colour titleTextColour { 0xFFD0592C };
    const Colour infoTextColour { 0xFFC8D02C };

    const Colour progressBarColour { 0x90C8D02C };

    const Colour controlTextColour { 0xFF0EDED4 };
    const Colour osMenuAccentColour { 0xFFEAA92C };
} // namespace Colours
} // namespace GUIConstants